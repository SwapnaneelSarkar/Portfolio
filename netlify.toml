[build]
  command = "chmod +x build.sh && ./build.sh"
  publish = "build/web"

# SPA fallback — the app uses path URLs (PathUrlStrategy), so deep links
# like /projects must serve index.html.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
