[build]
  command = "chmod +x build.sh && ./build.sh"
  publish = "build/web"

# Redirects live in web/_redirects, not here. Netlify processes netlify.toml
# rules ahead of _redirects, so a catch-all in this file would shadow the
# per-route rewrites that tool/prerender_routes.py generates.
